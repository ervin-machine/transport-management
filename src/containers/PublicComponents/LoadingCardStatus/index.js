import React from 'react'

function LoadingCardStatus({ children }) {
  return (
    <div className='loading-card-status-content'>
        {children}
    </div>
  )
}

export default LoadingCardStatus